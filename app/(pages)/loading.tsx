import React from "react";

type Props = {};

const loading = (props: Props) => {
  return (
    <div className="center-col h-screen w-screen animate-ping">Loading...</div>
  );
};

export default loading;
