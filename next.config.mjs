/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "plus.unsplash.com",
      "via.placeholder.com",
      "images.unsplash.com",
      "img.kwcdn.com",
      "c.media-amazon.com",
    ],
  },
};

export default nextConfig;
